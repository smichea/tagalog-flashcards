# tagalog-flashcards
Fast learning app about tagalog vocabulary

## Setup

To regenerate `flashcards.csv`, install dependencies and run:

```bash
pip install -r requirements.txt
python generate_flashcards.py
```
